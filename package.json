{
  "name": "nexuss-ai-os",
  "version": "0.4.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "validate": "python scripts/validate_repository.py",
    "test": "python -m pytest tests",
    "lint": "ruff check . && mypy src services libs scripts"
  }
}
