{
  "name": "nexuss-ai-os",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "validate": "python scripts/validate_repository.py",
    "test": "python -m pytest tests",
    "lint": "ruff check . && mypy services libs scripts"
  }
}
